<% provide(:manage, 'Manage Users') %>
<div class="row">
  <div class="col-lg-12">
    <div class="panel panel-default">
      <div class="panel-heading">
        <p>List Users</p>
      </div>
      <div class="panel-body">
        <div class='header-panel-body'>
          <%= link_to raw('<i class="fa fa-plus"></i> Add user'), new_admin_user_path, class: 'button-item add-item' %>
          <%= form_tag admin_users_path, method: :get, class: 'col-lg-3', id: 'search'  do %>
            <%= search_field_tag 'content', '', { class: 'form-control', placeholder: 'Search' } %>
            <%= button_tag raw('<i class="fa fa-search"></i>'), class: 'btn btn-primary search-item' %>
          <% end %>
        </div>
        <% if @users.any? %>
          <table width="100%" class="table table-striped table-hover" id="dataTables-example">
            <thead>
              <tr>
                <th class="center">#</th>
                <th class="">Name</th>
                <th class="center">Email</th>
                <th class="center">Role</th>
                <th class="center">Address</th>
                <th class="center">Phone</th>
                <th class="center">Gender</th>
                <th class="center">Action</th>
              </tr>
            </thead>
            <tbody>
              <%= render @users %>
            </tbody>
          </table>
          <%= will_paginate %>
        <% else %>
          <p>No user</p>
        <% end %>
      </div>
    </div>
  </div>
</div>
