<% provide(:manage, 'Manage User') %>

<div class="row">
  <div class="col-lg-12">
    <div class="panel panel-default">
      <div class="panel-heading">
        <p>Update User</p>
      </div>
      <div class="panel-body">
        <div class="row">
          <div class="col-lg-6">
            <%= form_for(@user, url: admin_user_path) do |f| %>
              <%= form_group_for f, :name, label: false  do %>
                <div class="form-group">
                  <%= f.label :name %>
                  <%= f.text_field :name, placeholder: 'Name', class: 'form-control' %>
                </div>
              <% end %>
              <%= form_group_for f, :email, label: false  do %>
                <div class="form-group">
                  <%= f.label :email %>
                  <%= f.text_field :email, placeholder: 'Email', class: 'form-control' %>
                </div>
              <% end %>
              <%= form_group_for f, :password, label: false  do %>
                <div class="form-group">
                  <%= f.label :password %>
                  <%= f.password_field :password, placeholder: 'Password', class: 'form-control' %>
                </div>
              <% end %>
              <%= form_group_for f, :password, label: false  do %>
                <div class="form-group">
                  <%= f.label 'Password confirmation' %>
                  <%= f.password_field :password_confirm, placeholder: 'Password_confirmation', class: 'form-control' %>
                </div>
              <% end %>
              <div class="form-group">
                <%= f.label :role %>
                <%= f.radio_button :role, 'admin', :checked => true %> 
                <%= label :role_amdin, 'admin' %>
                <%= f.radio_button :role, 'user' %>
                <%= label :role_user, 'user' %>
              </div>
              <%= f.submit "Submit", class: 'btn btn-success' %>
              <%= f.submit "Reset", class: 'btn btn-warning', type: 'reset' %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
