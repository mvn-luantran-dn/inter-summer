<% provide(:manage, 'Manage Promotions Categories') %>
<div class="row">
  <div class="col-lg-12">
    <div class="panel panel-default">
      <div class="panel-heading">
        <p>List Promotions Categories of <%= @promotion.name %></p>
      </div>
      <div class="panel-body">
        <div class='header-panel-body'>
          <%= link_to raw('<i class="fa fa-plus"></i> Add Promotion Category'), new_admin_promotion_promotions_category_path(@promotion), class: 'add-item' %>
          <%= form_tag '#', class: 'col-lg-3', id: 'search'  do %>
            <%= search_field_tag 'name', '', { class: 'form-control', placeholder: 'Search' } %>
          <% end %>
        </div>
        <% if @promotions_categories.any? %>
          <table width="100%" class="table table-striped table-hover" id="dataTables-example">
            <thead>
              <tr>
                <th class="center"><input type="checkbox" id="checkAll"></th>
                <th class="">Category Name</th>
                <th class="center">Discount</th>
                <th class="center">Action</th>
              </tr>
            </thead>
            <tbody>
              <%= form_tag('#',:id => 'form_id', data: { confirm: "Your sure?" }) do %>
                <%= render @promotions_categories %>
              <% end %>
            </tbody>
          </table>
          <%= will_paginate %>          
          <%= link_to raw("<i class='fa fa-trash-o fa-2x'></i>"), "#", :onclick => "$('#form_id').submit()", class: "delete-more" %>
        <% else %>
          <p class="space-bottom">No Promotions Categories</p>
        <% end %>
      </div>
    </div>
  </div>
</div>
